{
  "name": "awesome-slms",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Curated aggregator of researched small language models, grouped by size class.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "node --test",
    "preview": "npm run build && node -e \"console.log('open dist/index.html')\"",
    "hf-sync": "node scripts/hf-sync.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.5.1"
  }
}
